import { LoadVideoDirective } from './load-video.directive';

describe('LoadVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadVideoDirective();
    expect(directive).toBeTruthy();
  });
});
